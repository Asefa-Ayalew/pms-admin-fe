'use client';

import { useParams } from "next/navigation";
import New{{pascalCase name}}TypeComponent from "../_component/new-{{dashCase name}}-component";

export default function New{{pascalCase name}}TypePage() {
    const params = useParams();

    return (
        <New{{pascalCase name}}TypeComponent editMode={params?.id==='new' ?'new':'detail'} />
    );
}