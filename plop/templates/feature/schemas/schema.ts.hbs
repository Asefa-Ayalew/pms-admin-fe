import z from 'zod';
export const New{{pascalCase name}}Schema = z.object({
    name: z.string({
        required_error: "{{pascalCase name}}`s Name is required",
    }),
    description: z.string().optional(),  
});